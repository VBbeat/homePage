function startGame(){
    gameStartTime_ms = (new Date()).getTime();
    gameStartFlg = true;
}

/**
 * Puzzle.reversePuzzle(col, row)を呼び出す
 * @param col 行
 * @param row 列 
 */
function doReversePuzzle(col, row) { }

/**
 * 指定されたidのタグに値を設定する
 * @param id タグのid
 * @param value タグに設定する値
 */
function setValueToHtml(id, value) { }

/**
 * すべての変数とインスタンスを初期化する
 */
function initAll() { }

/**
 * ゲームクリアした場合にアラートを表示する
 */
function showGameClearDialog() { }

